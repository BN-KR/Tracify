---
slug: python
title: Python SDK
description: Instrument Python agents with a decorator and nested spans.
section: Instrument
order: 3
---

# Python SDK

Install the package in the environment that runs your agent:

```bash
pip install tracify-sdk
```

```python
from tracify import trace_agent

@trace_agent()
def answer(question: str):
    return llm.complete(question, context=search_docs(question))
```

Set `TRACIFY_API_KEY` in production and attach session, user, environment, and release metadata when those dimensions matter to debugging.
