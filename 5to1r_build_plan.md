<p><strong>5to1r</strong></p><p>Comprehensive Build Plan</p><p><em>From MVP scaffold to production-ready platform</em></p><table><tr><td><p><strong>Where you are right now</strong></p><p>Next.js 16 scaffold ✓   Clerk auth ✓   Convex schema ✓   Tinybird datasource ✓   Inngest wired ✓   shadcn/ui ✓   Agent skills ✓</p></td></tr></table><p><em>The ingestion pipeline, trace viewer, SDK, and everything beyond are still to build.</em></p><h1><strong>1. Current State Audit</strong></h1><p>A clear picture of what is done, what is wired but untested, and what does not exist yet.</p><table><tr><td><p><strong>Component</strong></p></td><td><p><strong>What exists</strong></p></td><td><p><strong>Status</strong></p></td></tr><tr><td><p><strong>Next.js 16 App Router</strong></p></td><td><p>Project initialised, App Router configured, layout.tsx with ClerkProvider and ConvexClientProvider</p></td><td><p>✅ Done</p></td></tr><tr><td><p><strong>Clerk auth</strong></p></td><td><p>ClerkProvider in layout, middleware.ts protecting /dashboard and /runs, API routes excluded</p></td><td><p>✅ Done</p></td></tr><tr><td><p><strong>Convex schema</strong></p></td><td><p>projects, agentRuns, alerts tables with correct indexes. convex/ functions scaffolded</p></td><td><p>✅ Done</p></td></tr><tr><td><p><strong>Tinybird datasource</strong></p></td><td><p>spans.datasource defined with correct schema, partitioning, and sort keys</p></td><td><p>✅ Done</p></td></tr><tr><td><p><strong>Inngest</strong></p></td><td><p>Client singleton + inngest-functions.ts scaffolded. /api/inngest route exists</p></td><td><p>✅ Scaffolded</p></td></tr><tr><td><p><strong>Tinybird client</strong></p></td><td><p>tinybird.ts REST client exists</p></td><td><p>✅ Scaffolded</p></td></tr><tr><td><p><strong>POST /api/ingest</strong></p></td><td><p>route.ts exists — unknown if auth + Inngest emit are implemented</p></td><td><p>⚠️ Verify</p></td></tr><tr><td><p><strong>Agent skills (.agents/)</strong></p></td><td><p>26+ Vercel skills installed</p></td><td><p>✅ Done</p></td></tr><tr><td><p><strong>Trace viewer UI</strong></p></td><td><p>/runs/[runId] page — does not exist</p></td><td><p>❌ Not built</p></td></tr><tr><td><p><strong>Cost dashboard</strong></p></td><td><p>/dashboard — does not exist</p></td><td><p>❌ Not built</p></td></tr><tr><td><p><strong>Alert system</strong></p></td><td><p>Inngest function + Slack webhook — not built</p></td><td><p>❌ Not built</p></td></tr><tr><td><p><strong>Python SDK</strong></p></td><td><p>Does not exist</p></td><td><p>❌ Not built</p></td></tr><tr><td><p><strong>TypeScript SDK</strong></p></td><td><p>Does not exist</p></td><td><p>❌ Not built</p></td></tr><tr><td><p><strong>Project onboarding UI</strong></p></td><td><p>No signup → project creation → API key flow</p></td><td><p>❌ Not built</p></td></tr><tr><td><p><strong>Run replay</strong></p></td><td><p>Does not exist</p></td><td><p>❌ Phase 2</p></td></tr><tr><td><p><strong>Eval engine</strong></p></td><td><p>Does not exist</p></td><td><p>❌ Phase 3</p></td></tr></table><h1><strong>2. Environment &amp; Configuration Setup</strong></h1><p>Before writing a single line of product code, every environment variable and third-party service must be properly configured. This is the most common blocker.</p><h2><strong>2.1  Clerk</strong></h2><ul><li>Create application at clerk.com — choose Email + Google as sign-in methods</li><li>Copy NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY and CLERK_SECRET_KEY into .env.local</li><li>Enable Organisations in Clerk dashboard (Settings → Organizations) — required for team/project multi-tenancy</li><li>Create API Keys template in Clerk (for issuing 5t1r_ prefixed keys to SDK users) — or plan to store them in Convex and validate manually</li><li>Set sign-in and sign-up redirect URLs to /dashboard in Clerk dashboard</li></ul><h2><strong>2.2  Convex</strong></h2><ul><li>Run npx convex dev to provision your Convex deployment and get CONVEX_DEPLOYMENT and NEXT_PUBLIC_CONVEX_URL</li><li>Add both to .env.local</li><li>Confirm convex/schema.ts is deploying cleanly — check the Convex dashboard for the three tables</li><li>Set up Convex environment variables for secrets (TINYBIRD_TOKEN, SLACK_WEBHOOK_URL) via npx convex env set</li></ul><h2><strong>2.3  Tinybird</strong></h2><ul><li>Create free account at tinybird.co — choose EU or US region (pick once, cannot change)</li><li>Create a new Workspace called 5to1r</li><li>Push spans.datasource via the Tinybird CLI: tb push tinybird/spans.datasource</li><li>Create a read token (for queries from the app) and a write token (for ingestion) in Tinybird dashboard</li><li>Add TINYBIRD_TOKEN (write) and TINYBIRD_READ_TOKEN to .env.local and to Convex env</li><li>Create your first Pipe in Tinybird — spans_by_run.pipe — that queries SELECT * FROM spans WHERE runId = {runId:String} ORDER BY createdAt — this is what the trace viewer queries</li></ul><h2><strong>2.4  Inngest</strong></h2><ul><li>Create account at inngest.com and create an app called 5to1r</li><li>Copy INNGEST_SIGNING_KEY and INNGEST_EVENT_KEY into .env.local</li><li>Verify the /api/inngest route is correctly registered — open http://localhost:3000/api/inngest in the browser, it should return a 200 with Inngest SDK info</li><li>In development, run the Inngest Dev Server alongside Next.js: npx inngest-cli@latest dev</li></ul><h2><strong>2.5  Full .env.local checklist</strong></h2><table><tr><td><p><strong>Required environment variables — all must be set before any feature works</strong></p><p>NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...</p><p>CLERK_SECRET_KEY=sk_test_...</p><p>NEXT_PUBLIC_CONVEX_URL=https://....convex.cloud</p><p>CONVEX_DEPLOYMENT=dev:...</p><p>TINYBIRD_TOKEN=p.ey...          (write token — ingestion)</p><p>TINYBIRD_READ_TOKEN=p.ey...     (read token — queries)</p><p>INNGEST_SIGNING_KEY=signkey-...</p><p>INNGEST_EVENT_KEY=...</p><p>SLACK_WEBHOOK_URL=https://hooks.slack.com/...   (optional at MVP)</p></td></tr></table><h1><strong>3. Phase 1 — The Ingestion Pipeline (Build First)</strong></h1><p>This is the critical path. Nothing else in the product works until spans can flow from an agent, through the API, into Tinybird and Convex. Build and test this end-to-end before touching any UI.</p><h2><strong>3.1  Verify and complete POST /api/ingest</strong></h2><p>Open src/app/api/ingest/route.ts and ensure the following logic is fully implemented:</p><ol><li>Extract Bearer token from Authorization header. Return 401 if missing.</li><li>Query Convex to find a project matching the API key: ctx.db.query('projects').withIndex('by_apiKey', q =&gt; q.eq('apiKey', token)).first()</li><li>Return 401 if no project found.</li><li>Parse and validate the request body — check that spanId, runId, spanType, input, output, latencyMs, costUsd, createdAt are all present.</li><li>Emit an Inngest event: await inngest.send({ name: '5to1r/span.received', data: { ...span, projectId } })</li><li>Return 202 Accepted immediately. Never make the SDK wait for the write to complete.</li></ol><h2><strong>3.2  Build the Inngest processSpan function</strong></h2><p>This runs asynchronously after the API route returns. Open src/lib/inngest-functions.ts and implement:</p><ol><li>Receive the 5to1r/span.received event.</li><li>Write the span row to Tinybird via POST to https://api.tinybird.co/v0/events?name=spans with the span payload as NDJSON. Use the TINYBIRD_TOKEN.</li><li>Upsert the agentRun in Convex: if runId exists, increment spanCount and update totalCostUsd. If not, create it with status 'running'.</li><li>Check alert thresholds: if totalCostUsd &gt; project.costThreshold, emit a 5to1r/alert.triggered event.</li><li>Mark run as completed when a span with spanType === 'run_end' is received.</li></ol><h2><strong>3.3  Build Convex mutations for agentRuns</strong></h2><ul><li>upsertRun(runId, projectId, costDelta, spanCount) — creates or updates an agentRun record</li><li>completeRun(runId, status) — sets finishedAt and final status</li><li>getRun(runId) — returns the full run record (used by trace viewer)</li><li>listRunsByProject(projectId, limit) — returns recent runs for the dashboard</li></ul><h2><strong>3.4  Build Convex mutations for projects</strong></h2><ul><li>createProject(name, clerkOrgId) — generates a 5t1r_ prefixed API key, stores the project</li><li>getProjectByApiKey(apiKey) — used by the ingest route for auth</li><li>getProjectsByOrg(clerkOrgId) — lists all projects for the logged-in org</li></ul><h2><strong>3.5  Test the pipeline end-to-end</strong></h2><p>Before building any UI, manually test the full pipeline with curl:</p><table><tr><td><p><strong>Test the pipeline with a curl command</strong></p><p>curl -X POST http://localhost:3000/api/ingest \</p><p>  -H "Authorization: Bearer 5t1r_your_api_key" \</p><p>  -H "Content-Type: application/json" \</p><p>  -d '{"spanId":"test-001","runId":"run-001","spanType":"llm_call",</p><p>        "input":"Hello","output":"World","latencyMs":420,</p><p>        "costUsd":0.000400,"modelId":"claude-sonnet-4-5",</p><p>        "toolName":"","createdAt":"2025-01-01T12:00:00Z"}'</p><p>Expected: 202 Accepted</p><p>Then check: Tinybird → spans table has 1 row</p><p>Then check: Convex dashboard → agentRuns table has 1 row with runId=run-001</p></td></tr></table><h1><strong>4. Phase 1 — Onboarding &amp; Project UI</strong></h1><p>The first thing a new user sees after signing up. Must be fast, clear, and end with them holding an API key they can paste into their agent.</p><h2><strong>4.1  Landing page — src/app/page.tsx</strong></h2><ul><li>Replace the default Next.js page with a proper landing page</li><li>Hero: '5to1r — Agent observability for developers'. One sentence. Two CTAs: 'Get started free' and 'View docs'</li><li>Three feature blocks: Real-time trace viewer / Cost tracking / Zero-config SDK</li><li>Code snippet showing a 5-line SDK install — the fastest possible time-to-value illustration</li><li>Clerk SignInButton / SignUpButton in the nav — redirect to /dashboard after auth</li></ul><h2><strong>4.2  Dashboard shell — src/app/dashboard/page.tsx</strong></h2><ul><li>Protected by Clerk middleware (already configured)</li><li>On first visit (no projects exist): show 'Create your first project' empty state</li><li>Create Project modal: input for project name → calls createProject Convex mutation → returns API key</li><li>Display the generated API key once, with a copy button. Show a warning that it will not be shown again</li><li>If projects exist: show list of projects with run counts and last active timestamp</li></ul><h2><strong>4.3  Project settings page — src/app/dashboard/[projectId]/settings/page.tsx</strong></h2><ul><li>Show project name, created date, API key (masked, with reveal + copy button)</li><li>Alert thresholds: cost per run (USD), max duration (seconds) — saved to Convex</li><li>Danger zone: delete project (soft delete, confirm dialog)</li></ul><h2><strong>4.4  Navigation</strong></h2><ul><li>Sidebar with: Dashboard, Runs, Settings, Docs link</li><li>Project switcher at top of sidebar if org has multiple projects</li><li>UserButton from Clerk in the bottom-left corner — handles sign out, account management</li></ul><h1><strong>5. Phase 1 — The Trace Viewer (Core Product)</strong></h1><p>This is the single most important UI in the product. It is the reason developers will adopt 5to1r. It must be fast, real-time, and show the full picture of a run in one screen.</p><h2><strong>5.1  Runs list — src/app/dashboard/[projectId]/runs/page.tsx</strong></h2><ul><li>Convex useQuery hook subscribing to listRunsByProject — live updates as new runs arrive</li><li>Table: Run ID, Status badge (running/completed/failed), Span count, Total cost (USD), Duration, Started at</li><li>Status badge green/amber/red with a pulsing dot for 'running' state</li><li>Click any row to navigate to /dashboard/[projectId]/runs/[runId]</li><li>Filter bar: by status, date range, cost range</li><li>Pagination — default 25 per page</li></ul><h2><strong>5.2  Run detail — src/app/dashboard/[projectId]/runs/[runId]/page.tsx</strong></h2><ul><li>This page has two data sources: Convex (run metadata, live) and Tinybird (span rows, queried on load).</li><li>Run header: Run ID, status badge, total cost, total duration, span count, started/finished times</li><li>If status is running: show live updating cost and span count via Convex subscription</li><li>Fetch spans from Tinybird via the spans_by_run pipe</li><li>Render as a vertical timeline with cards and left border coloured by spanType</li><li>Span types and colours: llm_call (indigo), tool_call (green), decision (amber), error (red), run_end (gray)</li><li>Each span card shows: spanType badge, modelId or toolName, latencyMs, costUsd, timestamp</li><li>Click a span card to expand: show full input JSON and output JSON in syntax-highlighted code blocks</li><li>Show a Recharts horizontal bar chart at the top — one bar per span, width = latencyMs, colour = spanType</li><li>Run summary sidebar: cost breakdown, latency breakdown, model usage, tool usage</li></ul><h2><strong>5.3  Tinybird Pipes to create</strong></h2><ul><li>spans_by_run — SELECT * FROM spans WHERE runId = {runId:String} ORDER BY createdAt</li><li>cost_by_project_daily — SELECT toDate(createdAt) as date, sum(costUsd) as total FROM spans WHERE projectId = {projectId:String} GROUP BY date ORDER BY date</li><li>spans_by_type — SELECT spanType, count() as count, avg(latencyMs) as avgLatency, sum(costUsd) as totalCost FROM spans WHERE runId = {runId:String} GROUP BY spanType</li><li>recent_runs_summary — SELECT runId, count() as spanCount, sum(costUsd) as totalCost, min(createdAt) as startedAt, max(createdAt) as finishedAt FROM spans WHERE projectId = {projectId:String} GROUP BY runId ORDER BY startedAt DESC LIMIT 50</li></ul><h1><strong>6. Phase 1 — Cost &amp; Usage Dashboard</strong></h1><p>Developers need to understand their agent costs immediately. This dashboard is a key retention driver — people check it daily once they are paying for LLM API calls.</p><h2><strong>6.1  Dashboard page — src/app/dashboard/[projectId]/page.tsx</strong></h2><ul><li>Stat cards: total spend today, total spans today, active runs right now, failed runs today</li><li>Cost over time chart: Recharts LineChart, last 30 days, toggleable by spanType</li><li>Date range picker: 7d / 30d / 90d</li><li>Model cost breakdown: Tremor BarChart by model name</li><li>Run cost distribution: histogram highlighting outlier expensive runs</li><li>Recent runs table: last 10 runs with status, cost, duration — click to open trace viewer</li></ul><h1><strong>7. Phase 1 — Alert System</strong></h1><p>Alerts are the feature that makes 5to1r feel like production infrastructure rather than a debug tool. A developer should know their agent failed before their users do.</p><h2><strong>7.1  Alert Inngest function</strong></h2><ul><li>Listen for 5to1r/alert.triggered events emitted by the processSpan function</li><li>Write the alert to Convex alerts table with: runId, type, message, triggeredAt, projectId</li><li>Send Slack notification via SLACK_WEBHOOK_URL if configured</li><li>Send email via Clerk or Resend to the project owner if Slack is not configured</li></ul><h2><strong>7.2  Alert types to implement</strong></h2><ul><li>run_failed: a span with spanType error and no subsequent non-error spans</li><li>cost_exceeded: totalCostUsd for the run has passed the project costThreshold setting</li><li>duration_exceeded: time since startedAt exceeds maxDurationSeconds with no run_end span</li><li>run_stalled: no span received for a running run in the past 5 minutes</li></ul><h2><strong>7.3  Alerts UI</strong></h2><ul><li>Alerts bell icon in the sidebar nav with unread count badge</li><li>Alerts page at /dashboard/[projectId]/alerts — newest first</li><li>Each alert: type badge, message, timestamp, link to the offending run</li><li>Mark as resolved button — updates Convex record</li><li>Alert settings in project settings page</li></ul><h1><strong>8. Phase 1 — The SDKs (Python + TypeScript)</strong></h1><p>The SDKs are the product's front door. Every developer interaction with 5to1r starts with pip install or npm install. They must be dead simple — one import, one decorator, done.</p><h2><strong>8.1  Python SDK — packages/python-sdk/</strong></h2><ul><li>Create packages/python-sdk/ or separate repo at 5to1r/sdk-python</li><li>Structure: src/fivetoone/, __init__.py, client.py, span.py, decorators.py, exceptions.py</li><li>Install: pip install 5to1r</li><li>@trace_agent decorator wraps the function, generates runId, and sets up span capture</li><li>Provide span() context manager and llm_call(), tool_call(), decision() helpers</li><li>All POSTs are async — never block the agent</li><li>Retry failed POSTs up to 3 times with exponential backoff — never crash the agent</li><li>Configurable via environment variable FIVETOONE_API_KEY</li><li>Send run_end span automatically when decorated function returns or raises</li><li>Capture exception details in run_end output if agent raises</li><li>Publish to PyPI as 5to1r, use pyproject.toml, GitHub Actions, minimum Python 3.9</li></ul><h2><strong>Python SDK usage example</strong></h2><table><tr><td><p><strong>Python SDK usage example</strong></p><p>from fivetoone import trace_agent, FiveToOneClient</p><p>client = FiveToOneClient(api_key="5t1r_your_key")</p><p>@trace_agent(client=client)</p><p>def my_agent(goal: str) -&gt; str:</p><p>    # your agent code here</p><p>    return result</p></td></tr></table><h2><strong>8.2  TypeScript SDK — packages/ts-sdk/</strong></h2><ul><li>Install: npm install @5to1r/sdk</li><li>Export FiveToOneClient, traceAgent, llmCall, toolCall, decision</li><li>Use native fetch in Node 18+ — no dependencies</li><li>traceAgent wraps async functions, generates runId, sends run_end on completion</li><li>All span POSTs are fire-and-forget</li><li>Reads FIVETOONE_API_KEY from process.env</li><li>Publish to npm as @5to1r/sdk</li><li>Export full TypeScript types and use GitHub Actions for tsc, jest, npm publish</li></ul><h2><strong>8.3  SDK documentation</strong></h2><ul><li>README.md with 5-minute quickstart for both Python and TypeScript</li><li>Working demo example that sends spans to a demo project</li><li>CHANGELOG.md starting from 0.1.0</li><li>CONTRIBUTING.md for tests and PRs</li></ul><h1><strong>9. Phase 2 — Run Replay</strong></h1><p>Run replay is the single feature that separates 5to1r from a logging tool. The ability to step through a past agent run interactively — seeing exactly what the agent saw at each step — is worth the engineering effort.</p><h2><strong>9.1  Data requirements</strong></h2><ul><li>Full span input/output JSON must be stored</li><li>For very large payloads, store truncated version in Tinybird and full version in S3</li><li>Implement S3 upload in processSpan when input/output JSON &gt; 10KB</li></ul><h2><strong>9.2  Replay UI</strong></h2><ul><li>Add a Replay run button to run detail page</li><li>Replay mode shows spans one at a time with Previous / Next / Play controls</li><li>Each step shows full input, output, model/tool used</li><li>Play mode auto-advances every 1.5 seconds</li><li>Highlight the current span in the main timeline while replaying</li></ul><h2><strong>9.3  Re-run a span (advanced)</strong></h2><ul><li>Allow developer to edit span input and re-run it against same model</li><li>Call LLM API from a Next.js server action with developer API key</li><li>Show new output alongside original — A/B comparison</li><li>Phase 2b only after replay is complete</li></ul><h1><strong>10. Phase 2 — Orchestrator &amp; Runtime Layer</strong></h1><p>Once developers are using the trace layer daily, the runtime layer is the natural upsell — they want 5to1r to not just observe their agents but to run them reliably.</p><h2><strong>10.1  Orchestrator SDK extension</strong></h2><ul><li>Extend SDKs with an Agent class</li><li>Agent(goal, tools, memory, max_cost_usd, model)</li><li>run(input) executes the agent with 5to1r managing the step loop</li><li>Automatic cost ceiling, retry, model fallback</li></ul><h2><strong>10.2  Tool registry</strong></h2><ul><li>Tool registration API: client.register_tool(name, function, description, schema)</li><li>Versioned tools with stable tested versions</li><li>Tool auth and token refresh</li><li>Tool registry UI with versions, call counts, error rates</li></ul><h2><strong>10.3  Memory manager</strong></h2><ul><li>Short-term memory: smart context window management</li><li>Long-term memory: vector store integration</li><li>Episodic memory: relevant past runs surfaced to new runs</li><li>Memory backend: Convex + Upstash Vector</li></ul><h1><strong>11. Phase 3 — Eval Engine</strong></h1><p>The eval engine is the retention moat. Once a team has written their agent test suite through 5to1r, the switching cost becomes enormous.</p><h2><strong>11.1  Eval SDK</strong></h2><ul><li>Evals defined in code alongside the agent</li><li>client.eval(name, input, assertions)</li><li>Assertions: called_before, never_called, output_contains, cost_under, completed_in</li><li>Run evals manually: npx 5to1r eval run</li><li>Run evals in CI and fail build if eval fails</li></ul><h2><strong>11.2  Eval runner</strong></h2><ul><li>Store eval runs in Convex evalRuns table</li><li>Eval results UI: pass/fail per assertion, diffs between runs</li><li>Baseline tracking and automatic regression detection</li></ul><h2><strong>11.3  Enterprise controls</strong></h2><ul><li>SSO via Clerk Enterprise</li><li>Audit log of key use, trace views, settings changes</li><li>Role-based access: Admin, Developer, Viewer</li><li>Data residency and custom retention</li></ul><h1><strong>12. Docs &amp; Developer Experience</strong></h1><p>For a developer tool, documentation IS the product. A developer who cannot get a span into 5to1r within 5 minutes will not come back.</p><h2><strong>12.1  Docs site</strong></h2><ul><li>Use Mintlify or Nextra</li><li>Docs at docs.5to1r.com</li><li>Structure: Quickstart / Concepts / Python SDK / TypeScript SDK / API Reference / Guides</li></ul><h2><strong>12.2  Quickstart</strong></h2><ul><li>Sign up at 5to1r.com</li><li>Create project and copy API key</li><li>pip install 5to1r or npm install @5to1r/sdk</li><li>Add two lines to your agent</li><li>Run agent and open trace viewer</li></ul><h2><strong>12.3  API Reference</strong></h2><ul><li>OpenAPI spec for POST /api/ingest</li><li>Span payload schema with field descriptions and examples</li><li>Error codes: 401, 402, 422, 429, 500</li></ul><h2><strong>12.4  Guides</strong></h2><ul><li>How to instrument a LangChain agent with 5to1r</li><li>How to instrument a Vercel AI SDK agent with 5to1r</li><li>Debugging a runaway cost agent</li><li>Setting up eval tests in CI</li><li>Understanding token costs</li></ul><h1><strong>13. Infrastructure &amp; Deployment</strong></h1><h2><strong>13.1  Vercel deployment</strong></h2><ul><li>Connect GitHub repo to Vercel</li><li>Add all env vars</li><li>Production domain: 5to1r.com</li><li>Preview deployments for every PR</li><li>Configure Inngest production /api/inngest URL</li></ul><h2><strong>13.2  Convex production deployment</strong></h2><ul><li>Run npx convex deploy</li><li>Set all environment variables on production Convex deployment</li><li>Convex is globally distributed — no region selection needed</li></ul><h2><strong>13.3  Tinybird production</strong></h2><ul><li>Tinybird prod workspace is already production-grade</li><li>Enable workspace backups</li><li>Set billing alert for ingestion spikes</li></ul><h2><strong>13.4  Monitoring 5to1r itself</strong></h2><ul><li>Vercel Analytics</li><li>Sentry via @sentry/nextjs</li><li>Inngest built-in monitoring</li><li>Uptime monitoring via Better Uptime or UptimeRobot</li><li>Create /api/health route</li></ul><h1><strong>14. Go-to-Market Tasks</strong></h1><p>Shipping the product is not enough. Getting developers to know about it, try it, and tell others is a parallel workstream that starts before launch.</p><h2><strong>14.1  Pre-launch</strong></h2><ul><li>Set up coming soon page and email waitlist</li><li>Open source SDK repos immediately</li><li>Start build-in-public thread with weekly screenshots</li><li>Join Latent Space Discord, AI Engineer Discord, r/MachineLearning, r/LocalLLaMA</li><li>Write: Why AI agents fail in production and what we are doing about it</li></ul><h2><strong>14.2  Launch day</strong></h2><ul><li>Product Hunt launch assets: logo, screenshots, 60-second demo video, tagline</li><li>Hacker News Show HN post focused on problem not features</li><li>Post demo video on X/Twitter, LinkedIn, Discords</li><li>Respond to every comment within one hour</li><li>Have a 10-second Loom demo ready</li></ul><h2><strong>14.3  Post-launch growth</strong></h2><ul><li>Reach out directly to first 20 developers</li><li>Write one technical article per week</li><li>Add Share trace button for public read-only links</li><li>Integrate with LangChain, LlamaIndex, Vercel AI SDK</li><li>Apply to Y Combinator</li></ul><h1><strong>15. Monetisation Setup</strong></h1><h2><strong>15.1  Stripe integration</strong></h2><ul><li>Create Stripe account and products: Free, Pro ($49/month), Team ($299/month)</li><li>Install Stripe packages</li><li>Create webhook handler, checkout session creator, portal session creator</li><li>Store Stripe customer ID and subscription status in Convex</li><li>Enforce plan limits in processSpan</li><li>Upgrade prompt when approaching plan limits</li></ul><h1><strong>15.2 Plan limits to enforce</strong></h1><table><tr><td><p><strong>Feature</strong></p></td><td><p><strong>Free</strong></p></td><td><p><strong>Pro</strong></p></td><td><p><strong>Team</strong></p></td></tr><tr><td><p><strong>Spans per month</strong></p></td><td><p>50,000</p></td><td><p>5,000,000</p></td><td><p>50,000,000</p></td></tr><tr><td><p><strong>Trace history</strong></p></td><td><p>7 days</p></td><td><p>90 days</p></td><td><p>1 year</p></td></tr><tr><td><p><strong>Run replay</strong></p></td><td><p>No</p></td><td><p>Yes</p></td><td><p>Yes</p></td></tr><tr><td><p><strong>Eval engine</strong></p></td><td><p>No</p></td><td><p>No</p></td><td><p>Yes</p></td></tr><tr><td><p><strong>Alert channels</strong></p></td><td><p>Email only</p></td><td><p>Email + Slack</p></td><td><p>Email + Slack</p></td></tr><tr><td><p><strong>Team members</strong></p></td><td><p>1</p></td><td><p>5</p></td><td><p>Unlimited</p></td></tr><tr><td><p><strong>Projects</strong></p></td><td><p>1</p></td><td><p>10</p></td><td><p>Unlimited</p></td></tr><tr><td><p><strong>Price</strong></p></td><td><p>Free</p></td><td><p>$49/month</p></td><td><p>$299/month</p></td></tr></table><h1><strong>16. Master Task Checklist — Prioritised</strong></h1><p>Every task needed to ship the MVP, in order. Work top to bottom. Do not skip ahead.</p><h2><strong>WEEK 1 — Environment &amp; Pipeline</strong></h2><ol><li>Set up Clerk application, copy keys to .env.local</li><li>Run npx convex dev, confirm three tables appear in Convex dashboard</li><li>Create Tinybird account, push spans.datasource via CLI, create read + write tokens</li><li>Create Inngest app, copy keys to .env.local, confirm /api/inngest returns 200</li><li>Implement createProject Convex mutation</li><li>Verify POST /api/ingest</li><li>Implement processSpan Inngest function</li><li>End-to-end test: curl → 202 → Tinybird row → Convex row</li></ol><h2><strong>WEEK 2 — Core UI</strong></h2><ol><li>Build landing page</li><li>Build dashboard shell with sidebar navigation</li><li>Build Create project flow + API key modal</li><li>Create spans_by_run Tinybird Pipe</li><li>Build runs list page with Convex live query</li><li>Build run detail page</li><li>Span card expand/collapse with JSON display</li><li>Run summary sidebar</li></ol><h2><strong>WEEK 3 — Dashboard, Alerts, SDKs</strong></h2><ol><li>Create Tinybird Pipes for cost and spans</li><li>Build cost dashboard</li><li>Implement alert Inngest function</li><li>Implement Slack webhook notification</li><li>Build alerts UI</li><li>Build Python SDK</li><li>Build TypeScript SDK</li><li>Publish Python SDK to PyPI</li><li>Publish TypeScript SDK to npm</li></ol><h2><strong>WEEK 4 — Polish, Docs, Launch</strong></h2><ol><li>Write SDK README</li><li>Set up docs site</li><li>Write Quickstart and SDK docs</li><li>Record 60-second demo video</li><li>Set up Vercel deployment and domain</li><li>Deploy Convex to production</li><li>Set up Sentry and uptime monitoring</li><li>Prepare Product Hunt assets</li><li>Prepare Hacker News Show HN post</li><li>Launch</li></ol><h2><strong>POST-LAUNCH — Phase 2</strong></h2><ol><li>Stripe integration</li><li>S3 integration for large span payloads</li><li>Run replay UI</li><li>Orchestrator SDK extension</li><li>Tool registry</li><li>Short-term memory manager</li><li>LangChain integration plugin</li><li>Vercel AI SDK integration plugin</li></ol><h2><strong>PHASE 3 — When paying teams are live</strong></h2><ol><li>Eval framework</li><li>Long-term memory</li><li>SSO via Clerk Enterprise</li><li>Audit logs</li><li>Role-based access control</li><li>Data residency options</li><li>Multi-agent coordination</li></ol><h1><strong>17. The One Thing</strong></h1><table><tr><td><p><strong>Ship the pipeline first. Nothing else matters until spans flow.</strong></p><p><em>Every hour spent on the trace viewer UI before the pipeline works is wasted. The moment you can run a curl command and see a span appear in Tinybird in real time — that is the moment 5to1r becomes real. Build that first. Build everything else second.</em></p></td></tr></table>